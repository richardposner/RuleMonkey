# Revisions in this file are skipped by `git blame --ignore-revs-file`.
# GitHub's web blame view honors this file automatically; for local
# `git blame`, opt in once per clone with:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add only commits that are pure-formatting / pure-rename changes.

# Apply pre-commit formatting baseline (clang-format, ruff-format, EOF/whitespace)
6c18bc5a9c6caa2bede02a8f091e084c79c0ee05
